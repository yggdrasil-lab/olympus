# ==============================================================================
# OLYMPUS CONFIGURATION FILE (.env)
# ==============================================================================
# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in the required values below.
# 3. For development, ensure your DNS (e.g., AdGuard Home) points subdomains to your host.
# ==============================================================================

# --- [DOMAIN & CLOUDFLARE SETTINGS] ---
# Your base domain (e.g., tienzo.net)
CLOUDFLARE_DOMAIN=yourdomain.com

# --- [SERVICE SUBDOMAINS] ---
# These will be prefixed to CLOUDFLARE_DOMAIN (e.g., traefik.yourdomain.com)
TRAEFIK_SUBDOMAIN=traefik
PORTAINER_SUBDOMAIN=portainer

# Cloudflare account email
CLOUDFLARE_EMAIL=your-email@example.com

# Cloudflare API Token (Requires DNS:Edit permissions for the domain)
# Create one at: https://dash.cloudflare.com/profile/api-tokens
CF_DNS_API_TOKEN=YOUR_CLOUDFLARE_DNS_API_TOKEN

# --- [PRODUCTION / DEPLOYMENT SETTINGS] ---
# Cloudflare Tunnel Token
# Required for docker-compose.prod.yml (Traefik -> Cloudflare Tunnel)
# Get your token from the Cloudflare Zero Trust dashboard: Access -> Tunnels
TUNNEL_TOKEN=YOUR_TUNNEL_TOKEN_HERE
