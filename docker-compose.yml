version: '3.8'

networks:
  aether-net:
    external: true

services:
  traefik:
    image: "traefik:v2.10"
    container_name: "traefik"
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    ports:
      - "80:80"   # HTTP
      - "443:443" # HTTPS
      - "8080:8080" # Traefik Dashboard (local access only)
    command:
      # Core settings
      - "--log.level=INFO"
      - "--api.insecure=true"
      # Providers
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      # Entrypoints
      - "--entrypoints.web.address=:80"
      - "--entrypoints.websecure.address=:443"
      - "--entrypoints.traefik.address=:8080"
      # Global HTTP to HTTPS redirection
      - "--entrypoints.web.http.redirections.entrypoint.to=websecure"
      - "--entrypoints.web.http.redirections.entrypoint.scheme=https"
      - "--entrypoints.web.http.redirections.entrypoint.permanent=true"
      # Add Cloudflare DNS Challenge Provider for Let's Encrypt
      - "--certificatesresolvers.cloudflare.acme.dnschallenge=true"
      - "--certificatesresolvers.cloudflare.acme.dnschallenge.provider=cloudflare"
      - "--certificatesresolvers.cloudflare.acme.email=${CLOUDFLARE_EMAIL}"
      - "--certificatesresolvers.cloudflare.acme.storage=/letsencrypt/acme.json"
    environment:
      - "CLOUDFLARE_EMAIL=${CLOUDFLARE_EMAIL}"
      - "CLOUDFLARE_API_KEY=${CLOUDFLARE_API_KEY}"
    networks:
      - aether-net
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock:ro"
      - "traefik-data:/letsencrypt" # Persist ACME certificates

  cloudflared:
    image: "cloudflare/cloudflared:latest"
    container_name: "cloudflared"
    restart: unless-stopped
    depends_on:
      - traefik
    environment:
      - TUNNEL_TOKEN=${TUNNEL_TOKEN}
    command: tunnel run
    networks:
      - aether-net
    labels:
      - "traefik.enable=false"

  whoami:
    image: "traefik/whoami"
    container_name: "whoami"
    restart: unless-stopped
    networks:
      - aether-net
    labels:
      - "traefik.enable=true"
      # Define a single service for the whoami container
      - "traefik.http.services.whoami-service.loadbalancer.server.port=80"

      # Router for local testing (HTTPS)
      - "traefik.http.routers.whoami-local.rule=Host(`whoami.localhost`)"
      - "traefik.http.routers.whoami-local.entrypoints=websecure"
      - "traefik.http.routers.whoami-local.tls=true"
      - "traefik.http.routers.whoami-local.service=whoami-service"

      # Router for public domain (HTTPS with Cloudflare)
      - "traefik.http.routers.whoami-public.rule=Host(`${SUBDOMAIN}.${CLOUDFLARE_DOMAIN}`)"
      - "traefik.http.routers.whoami-public.entrypoints=websecure"
      - "traefik.http.routers.whoami-public.service=whoami-service"
      - "traefik.http.routers.whoami-public.tls=true"
      - "traefik.http.routers.whoami-public.tls.certresolver=cloudflare"


volumes:
  traefik-data:
